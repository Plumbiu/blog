// import { genTocs } from '@/lib/toc'
import type { FC } from 'react'

interface Props {
  html: string
}

const Toc: FC<Props> = ({ html }) => {
  // genTocs(html)

  return <div></div>
}

export default Toc
